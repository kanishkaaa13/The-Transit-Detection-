# AstronomyAPI credentials (https://astronomyapi.com)
# Server-side only — these should NEVER be prefixed with VITE_ to keep them safe from the browser bundles.
ASTRONOMY_API_ID=your_application_id_here
ASTRONOMY_API_SECRET=your_application_secret_here
